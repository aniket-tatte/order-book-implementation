## To execute the project locally, please install Docker Desktop and make sure the Docker Kernel is running.
### Clone the project locally and execue the following command - 

```
docker-compose up -d
```
